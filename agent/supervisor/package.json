{
  "name": "merlin-supervisor",
  "version": "1.0.0",
  "description": "Stream-json supervisor for Merlin ops-agent. Spawns claude -p, parses events, dispatches messages over HTTP.",
  "type": "module",
  "bin": { "supervisor": "./index.mjs" },
  "scripts": {
    "start": "node index.mjs --agent ops",
    "dev": "node index.mjs --agent ops --model sonnet"
  },
  "engines": { "node": ">=20" }
}
