#!/usr/bin/env node
// Serendipity — deferred. Would surface "interesting nearby thing" based on
// location + taste when the user is out in a new area. Needs richer places
// scoring than we have today. Currently disabled in the registry; emits null
// with a note so the pipeline stays uniform.
import { emitNull } from "./_common.mjs";
emitNull("deferred — serendipity harvester not yet implemented");
